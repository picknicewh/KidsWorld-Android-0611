<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.hunme.school">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.OpenClassActivity" />
        <activity android:name=".activity.LeaveAskActivity" />
        <activity android:name=".activity.WebViewActivity" />
        <activity android:name=".activity.PublishInformActivity" />
        <activity android:name=".activity.PublishinfoChooseActivity"></activity>
    </application>

</manifest>